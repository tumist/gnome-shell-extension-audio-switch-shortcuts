<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
    <schema id="com.github.dbatis.audio-switch-shortcuts" path="/com/github/dbatis/audio-switch-shortcuts/">

        <key name="show-indicator" type="b">
            <default>true</default>
            <summary>Show indicator at taskbar</summary>
        </key>

        <key name="show-notifications" type="b">
            <default>true</default>
            <summary>Display notifications when switching audio devices.</summary>
        </key>

        <key name="cycle-input-hotkey" type="as">
            <default><![CDATA[['<Ctrl><Alt>=']]]></default>
            <summary>Keyboard shortcut to switch audio input device.</summary>
        </key>

        <key name="cycle-output-hotkey" type="as">
            <default><![CDATA[['<Ctrl><Alt>-']]]></default>
            <summary>Keyboard shortcut to switch audio output device.</summary>
        </key>

        <key name="audio-devices" type="s">
            <default>'[]'</default>
            <summary>Ordered list of audio devices.</summary>
            <description>
                JSON list of objects with fields: name, deviceType, cycled, active. Devices are
                not deleted from this list, so that their settings are available if a device
                was temporarily disconnected.
            </description>
        </key>

    </schema>
</schemalist>